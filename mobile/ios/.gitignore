# Generated by XcodeGen — the project is a pure function of project.yml.
Houston.xcodeproj/
Houston/Info.plist

# Staged at build time by the run-script phases (SDK bundle + design tokens).
Houston/Generated/

# Xcode build + user state
DerivedData/
build/
build-sim/

# Local-only dev screenshots (simulator captures, not source).
screenshots-dev/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside

.DS_Store
